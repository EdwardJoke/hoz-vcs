# Todo
> Target Version: v0.4.2 | Mode: Patch (installer + version source-of-truth)

## Must have

- [ ] **NEXT** M1: Make version dynamic from `build.zig.zon` — Currently hardcoded in 3 places:
  - [`src/final/final.zig:16`](src/final/final.zig#L16): `pub const VERSION = "0.4.2"`
  - [`src/main.zig:244`](src/main.zig#L244): `"hoz version 0.4.2"` in printVersion()
  - **Source of truth**: [`build.zig.zon:17`](build.zig.zon#L17): `.version = "0.4.2"`
  - **Action**: Use Zig's `@import("build.zig")` or build-time option to inject version at compile time, so changing `build.zig.zon` propagates everywhere automatically
  - **Why critical**: Without this, every release requires manual string sync across files — error-prone and already drifted once

- [ ] M2: Build full `install.sh` from ASCII art skeleton — Current [`install.sh`](install.sh) is only 7 echo lines of art. Needs:
  - OS detection (`uname -s` → Darwin/Linux)
  - Architecture detection (`uname -m` → x86_64/aarch64/arm64 mapping)
  - GitHub Release asset download via `curl`/`wget`
  - Install target: `~/.hoz/bin/hoz`
  - Post-install: print `export PATH="$HOME/.hoz/bin:$PATH"` instruction for user to add manually
  - Error handling: missing curl/wget, unsupported arch, network failure, permission denied
  - **Why critical**: This is the primary deliverable for v0.4.2 — without it, distribution story remains broken

## Should have

- [ ] S1: Installer robustness — Handle edge cases gracefully:
  - Check if `~/.hoz/bin` exists, create with `mkdir -p`
  - Detect existing installation and offer to overwrite/skip
  - Validate downloaded binary is executable (chmod +x)
  - Support `--version <tag>` flag for installing specific release
  - Support `--prefix <dir>` custom install path
  - **Why important**: A flaky installer damages user trust more than no installer

## Could have

- [ ] C1: Add uninstall helper — Print uninstall instructions or generate `uninstall.sh` snippet
- [ ] C2: Shell detection — Detect `.bashrc` vs `.zshrc` vs `.fish` and show tailored copy-paste commands

## Won't have (this version)

- [ ] W1: Auto-modify `.bashrc`/`.zshrc` — Explicitly excluded; user must add PATH manually for safety
- [ ] W2: Package manager integration (Homebrew, AUR, etc.) — Deferred to future minor release
- [ ] W3: Windows support — install.sh is POSIX-only; Windows needs separate approach
