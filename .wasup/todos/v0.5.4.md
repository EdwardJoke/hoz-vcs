# Todo
> Target Version: v0.5.4 | Mode: Bug (z+1)

## Must have

- [x] M1: Fix `install.sh` syntax error — Line 152 has malformed command substitution: `INSTALLED=$(""${PREFIX}/hoz" --version 2>/dev/null || echo "(version unknown)"")`. Extra quotes break the script. Fix to: `INSTALLED=$("${PREFIX}/hoz" --version 2>/dev/null || echo "(version unknown)")`.
  - **Why critical**: Installation script fails on every fresh install; users cannot install hoz

- [ ] **NEXT** M2: Fix remaining `openDirAbsolute` with relative paths — Same bug pattern as fixed in prior versions. Audit and replace `openDirAbsolute` calls passing relative paths (e.g., `".git"`) with `cwd.openDir()` or absolute path construction.
  - **Why critical**: Semantically incorrect API usage; may fail depending on runtime working directory

- [ ] M3: Fix memory leaks in `object_mod.parse()` callers — `parse()` allocates `owned_data` returned as `Object.data`, but callers never free `obj.data`. Add `Object.deinit(allocator)` and `defer obj.deinit(allocator)` after every `parse()` call.
  - **Why critical**: Memory leaks on every object parse operation; affects stability under load

- [ ] M4: Remove dead code from zlib refactor — Remove unused `Decompressor` struct from `src/compress/deflate.zig` (only used in its own tests), unused error variants (`ZlibError.InvalidChecksum`, `BadBlockType`, `CorruptData`, `DeflateError.UnsupportedFeature`), and redundant manual zlib header validation in `zlib.zig`.
  - **Why critical**: Dead code increases maintenance burden and misleads error handling

## Should have

- [ ] S1: Fix `formatDate` incomplete output in `cli/log.zig` — Current output shows only "Mon Day" (e.g., "Jan 5") but Git standard format is "Mon Jan 5 12:34:56 2025 +0000". Align `cli/log.zig` with `history/log.zig` full format.
  - **Why important**: Incomplete date display is a functional bug

- [ ] S2: Add `errdefer` guards to `object_mod.parseFromParts()` — The function allocates `full_header` and `full_content` but `errdefer` only covers `full_content`. If `full_content` allocation fails, `full_header` leaks.
  - **Why important**: Prevents memory leaks on allocation failure paths

## Could have

- [ ] C1: Audit silent error swallowing — Many call sites use `catch {}` or `catch return;` which silently discard errors. Add logging or proper error propagation to critical paths (object I/O, network operations, file writes).
  - **Why nice to have**: Improves debuggability but current behavior is functionally correct

- [ ] C2: Replace `crc32WithInitial` in `crc32.zig` with `std.hash.Crc32` streaming API — Custom bit-by-bit CRC32 implementation is slower than stdlib equivalent.
  - **Why nice to have**: Performance improvement but current implementation is correct

## Won't have (defer to v0.6.0)

- [ ] W1: Large repository performance optimizations — Commit history cache, incremental diff, optimized object lookup
- [ ] W2: Enhanced remote error recovery — Timeout handling, graceful degradation
- [ ] W3: GUI/IDE integration plugins — Out of scope for CLI-focused bugfix release
