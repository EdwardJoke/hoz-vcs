# Todo
> Target Version: v0.3.4 | Mode: Patch

## Must have

- [x] M1: Merge `checkout.zig` into `branch.zig` as subcommand — add `BranchAction.checkout` enum variant; move checkout logic from cli/checkout.zig into branch.zig's run() dispatch; update dispatcher.zig to remove standalone 'checkout' command (now handled by 'branch checkout' or 'branch out'); preserve all existing checkout functionality (branch switching, file restore, detached HEAD)
- [x] M2: Merge `switch.zig` into `branch.zig` as subcommand — add `BranchAction.switch_branch` enum variant; integrate switch.zig's create+switch logic (with -c, -C, --detach, --track options) into branch.zig; update dispatcher.zig to remove standalone 'switch' command (now 'branch switch'); ensure switch's BranchSwitcher integration works within branch context
- [x] M3: Update `dispatcher.zig` to remove Checkout and Switch imports — clean up the 62-command import list; remove 'checkout' and 'switch' from dispatch() if-else chain; add comment noting merged commands; verify no broken references to removed modules
- [x] M4: Update compatibility tests in `final/compat.zig` — replace all `runCheckout()` calls with `runHozCommand(&.{ "branch", "out", ... })` or equivalent new syntax; replace all `runSwitch()` calls with `runHozCommand(&.{ "branch", "switch", ... }); ensure 16+ test methods still pass with new command structure

## Should have

- [x] S1: Add backward-compatibility aliases (optional) — consider keeping 'checkout' and 'switch' as deprecated aliases that map to 'branch checkout' and 'branch switch' internally; or document breaking change clearly in release notes; decide on deprecation strategy ✅ Implemented: aliases with deprecation warnings
- [x] S2: Update help text and usage messages — ensure `hoz branch --help` shows all subcommands including new 'out/checkout' and 'switch'; update usage examples in command documentation comments ✅ Updated descriptions in main.zig

## Could have

- [ ] C1: Audit other command pairs for future merging — document which other commands could be consolidated (e.g., stash save/list/drop/apply, tag create/delete/list, remote add/remove/rename); create W1 wishlist for v0.3.5+

## Won't have (this version / defer to v0.3.5)

- [ ] W1: Full CLI framework refactor with base Command struct/mixin — keep as noted in v0.3.3 W1, requires broader API design discussion
- [ ] W2: Merge additional command pairs beyond branch/checkout/switch — stash, tag, remote, and other consolidations deferred to allow focused testing of this merge
