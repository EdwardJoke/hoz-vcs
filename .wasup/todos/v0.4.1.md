# Todo
> Target Version: v0.4.1 | Mode: Patch (bug fix release)

## Must have

- [ ] **NEXT** M1: Diagnose and fix all 6 test crashes — These are panics/segfaults that prevent tests from completing. Based on v0.4.0 metrics, crashes likely stem from:
  - Memory management issues (use-after-free, double-free) in tree builder or index operations
  - Null pointer dereferences in ref store or OID handling
  - Assertion failures from Zig 0.16 type system changes
  - **Action**: Run `zig build test`, capture crash stack traces, categorize root causes, fix systematically

- [ ] M2: Fix memory safety bugs in TreeBuilder — v0.4.0 fixed memory leak in `deinit()` but remaining crashes may indicate:
  - Uninitialized memory access in entry name handling
  - Buffer overflow in path construction
  - Incorrect allocator usage patterns
  - **Action**: Audit `src/tree/builder.zig` for remaining unsafe patterns after v0.4.0 fixes

- [ ] M3: Fix Index subsystem crashes — Index operations (addEntry, readEntry) had use-after-free bug fixed in v0.4.0. Remaining issues may include:
  - File I/O errors not properly handled
  - Incorrect stat/permission checks after std.fs migration
  - Path buffer overflows
  - **Action**: Audit `src/index/index.zig` and `src/index/index_entry.zig` for crash-prone patterns

## Should have

- [ ] S1: Improve test pass rate from 87.7% → 90%+ — Address subset of 38 failing tests, prioritizing:
  - Test assertion errors from API signature changes
  - Test data/format mismatches post-migration
  - Logic errors introduced during v0.4.0 refactoring
  - **Action**: Categorize failures, fix highest-impact ones first, aim for measurable improvement

- [ ] S2: Consolidate `resolveRef*()` variants (C1 deferred) — 8 duplicate implementations across:
  - `log.zig`, `archive.zig`, `rev_list.zig`, `pull.zig`
  - `request_pull.zig`, `ls_tree.zig`, `merge.zig`, `stash/save.zig`
  - **Benefit**: Reduces maintenance burden, ensures consistent behavior, eliminates copy-paste bugs

## Could have

- [ ] C1: Consolidate `updateHead()` variants (C2 deferred) — 3 copies across:
  - `commit.zig`, `head.zig` (canonical), `checkout/switch.zig`
  - **Benefit**: Single source of truth for head updates, easier to audit for correctness

- [ ] C2: Add integration test for crash-fix scenarios — Create targeted tests that exercise previously-crashing code paths to prevent regressions
  - **Benefit**: Regression safety net, documents expected behavior

## Won't have (defer to v0.5.0)

- [ ] W1: Full test suite pass (100%) — 38 failing tests is too large scope for patch release; target incremental improvement
- [ ] W2: Performance optimization — Focus only on correctness, not speed
- [ ] W3: New features or CLI commands — This is a pure bug fix release
- [ ] W4: Complete CLI framework refactor (W3 from v0.4.0) — Still deferred
