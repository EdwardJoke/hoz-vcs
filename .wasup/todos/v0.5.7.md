# Todo
> Target Version: v0.5.7 | Mode: Bug (UX Optimization)

## Must have

- [ ] **NEXT** M1: Unify error message format — Audit all CLI command error output paths, ensure `ErrorPolisher` or standard `fatal:` / `error:` prefix is used, replace bare `std.log.err` or context-free error returns. Key coverage: `cli/add.zig`, `cli/commit.zig`, `cli/push.zig`, `cli/pull.zig`, `cli/clone.zig`
- [ ] M2: Real repo integration test (~/hozt) — Write integration test scripts in `~/hozt` repo covering core command flow: init → add → commit → log → status → branch → checkout → diff → merge. Verify end-to-end behavior of command chains in real environment

## Should have

- [ ] S1: Help text completion — Add `--help` output for subcommands missing usage text (e.g., `bundle`, `notes`, `archive`, `am`), unify format as `usage: hoz <command> [options] [args]`
- [ ] S2: Critical path test coverage — Add unit tests for modules with low coverage: `workdir/status.zig`, `stage/stage.zig`, `reset/reset.zig`, `merge/three_way.zig`

## Could have

- [ ] C1: Progress indicators for long operations — Add progress display for clone/fetch/push network operations (leverage existing `Output.Progress` struct)
- [ ] C2: Color output detection optimization — Improve `OutputStyle.use_color` auto-detection (support `NO_COLOR` env var, pipe detection)

## Won't have (defer to v0.6.0)

- [ ] W1: Full porcelain/json output mode — Some commands lack machine-parseable output
- [ ] W2: Tab completion script generation — Feature enhancement scope
