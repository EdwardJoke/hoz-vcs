# Todo
> Target Version: v0.5.2 | Mode: Bug (z+1)

## Must have

- [x] M1: Fix `openDirAbsolute` with relative paths — Same bug as fixed in `cli/log.zig`. Replace `Io.Dir.openDirAbsolute(self.io, ".git", .{})` with absolute path construction via `currentPathAlloc + allocPrint` in:
  - `src/cli/commit.zig:59`
  - `src/cli/add.zig:68`
  - `src/final/benchmark.zig:286,360,375`
  - **Why critical**: `openDirAbsolute` expects absolute paths; passing `".git"` is semantically wrong and may fail depending on runtime state

- [ ] **NEXT** M2: Fix memory leak in `object_mod.parse()` callers — Add `Object.deinit(allocator)` method and call `defer obj.deinit(allocator)` after every `parse()` call. Affected files (76 call sites across 21 files):
  - `src/stash/pop.zig` (4 sites)
  - `src/stash/apply.zig` (4 sites)
  - `src/reset/hard.zig` (4 sites)
  - `src/reset/restore_source.zig` (1 site)
  - `src/cli/rebase.zig` (8 sites)
  - `src/cli/cherry_pick.zig` (6 sites)
  - `src/cli/revert.zig` (5 sites)
  - `src/cli/format_patch.zig` (3 sites)
  - `src/cli/checkout.zig` (3 sites)
  - `src/cli/branch.zig` (3 sites)
  - `src/cli/ls_tree.zig` (2 sites)
  - `src/cli/show.zig` (1 site)
  - `src/cli/cat_file.zig` (1 site)
  - `src/cli/filter_repo.zig` (1 site)
  - `src/describe/describe.zig` (1 site)
  - `src/object/tree.zig` (1 site)
  - `src/object/tag.zig` (1 site)
  - `src/object/commit.zig` (1 site)
  - `src/object/blob.zig` (1 site)
  - **Why critical**: Every call to `parse()` allocates `owned_data` that is never freed — memory leaks on every object parse operation

- [ ] M3: Clean up dead code from zlib refactor — Remove unused code left after switching `zlib.zig` decompression to `std.compress.flate.Decompress`:
  - Remove `Decompressor` struct from `src/compress/deflate.zig` (only used in its own tests; production code no longer calls it)
  - Remove `Decompressor`-only test functions from `deflate.zig`
  - Remove unused error variants: `ZlibError.InvalidChecksum`, `ZlibError.BadBlockType`, `ZlibError.CorruptData` from `zlib.zig`; `DeflateError.UnsupportedFeature` from `deflate.zig`
  - Remove redundant manual zlib header validation in `zlib.zig:decompress()` (lines 47-49) — `std.compress.flate.Decompress` with `.zlib` format handles this internally
  - **Why critical**: Dead code increases maintenance burden, confuses contributors, and unused error variants mislead error handling

## Should have

- [ ] S1: Fix `formatDate` incomplete output in `cli/log.zig` — Current output shows only "Mon Day" (e.g., "Jan 5") but Git standard format is "Mon Jan 5 12:34:56 2025 +0000". The `history/log.zig` version has the full format. Align `cli/log.zig` with `history/log.zig` format.
  - **Why important**: Incomplete date display is a functional bug that makes log output less useful

- [ ] S2: Add `errdefer` guards to `object_mod.parseFromParts()` — The function allocates `full_header` and `full_content` but the `errdefer` only covers `full_content`. If allocation of `full_content` fails, `full_header` leaks.
  - **Why important**: Prevents memory leaks on allocation failure paths

## Could have

- [ ] C1: Audit silent error swallowing — Many call sites use `catch {}` or `catch return;` which silently discard errors. Add logging or proper error propagation to critical paths (object I/O, network operations, file writes).
  - **Why nice to have**: Improves debuggability but current behavior is functionally correct

- [ ] C2: Replace `crc32WithInitial` in `crc32.zig` with `std.hash.Crc32` streaming API — Custom bit-by-bit CRC32 implementation is slower than stdlib equivalent.
  - **Why nice to have**: Performance improvement but current implementation is correct

## Won't have (defer to v0.6.0)

- [ ] W1: Large repository performance optimizations — Commit history cache, incremental diff, optimized object lookup (deferred from v0.5.1)
- [ ] W2: Enhanced remote error recovery — Timeout handling, graceful degradation (deferred from v0.5.1)
- [ ] W3: GUI/IDE integration plugins — Out of scope for CLI-focused bugfix release
