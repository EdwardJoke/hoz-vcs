# Todo
> Target Version: v0.3.3 | Mode: Patch

## Must have

- [ ] **NEXT** M1: Create shared `object/io.zig` with unified `readObject(git_dir, io, allocator, oid)` function — extract the OID→hex path→readFileAlloc→Zlib.decompress pattern used in 25+ locations; update all call sites in cli/commit.zig, cli/log.zig, cli/show.zig, cli/ls_tree.zig, cli/cat_file.zig, cli/cherry_pick.zig, cli/checkout.zig, cli/revert.zig, cli/format_patch.zig, cli/filter_repo.zig, cli/archive.zig, describe/describe.zig, clone/working_dir.zig, stash/apply.zig, reset/hard.zig, reset/restore_working.zig, reset/restore_source.zig, clean/gc.zig, blame/blame.zig; eliminates ~500 lines of duplication
- [ ] M2: Consolidate `resolveHead()` by reusing existing `commit/head.zig` — update HEAD file reading + "ref: " prefix detection + ref resolution logic; replace 8+ inline copies in cli/commit.zig, cli/log.zig, cli/rev_list.zig, cli/format_patch.zig, cli/filter_repo.zig, cli/archive.zig, describe/describe.zig, cli/name_rev.zig, blame/blame.zig; ensures consistent error handling across all consumers
- [ ] M3: Extract `makeMockCommit()` to `src/testing/mock.zig` — create shared test helper module with MockCommit factory and MockStore builder patterns; update merge/fast_forward.zig and merge/analyze.zig to import from shared location; removes ~30 lines of identical test code
- [ ] M4: Unify `writeLooseObject()` into single implementation — move to `object/io.zig` alongside readObject; update cli/commit.zig and cli/filter_repo.zig to call shared version; ensures consistent SHA1→dir creation→compress→write pipeline

## Should have

- [ ] S1: Refactor `GitCompatTester.run*()` methods in final/compat.zig — extract generic `setupRepo(name)` / `runGitCommand(args)` / `runHozCommand(args)` / `compareOutputs(git_out, hoz_out, msg)` helpers; collapse 16 near-identical 20-40 line methods into data-driven ~15-line test definitions each; eliminates ~300+ lines of copy-paste
- [ ] S2: Extract empty tree helper inside `cli/commit.zig writeTree()` — replace 4x repeated `"tree 0\x00"` → serialize → oidFromContent → writeLooseObject block with a single `fn getOrCreateEmptyTree(allocator, git_dir) !OID` method; reduces function from ~60 lines to ~35 lines

## Could have

- [ ] C1: Add line-count verification test — assert that net source lines decreased by ≥600 vs v0.3.2 baseline, preventing future re-growth of eliminated duplication

## Won't have (this version / defer to v0.4.0)

- [ ] W1: CLI Command base struct or mixin for init()/parseArgs()/run() boilerplate across 62 command files — requires broader API design discussion and risks breaking existing command structure
