# Todo
> Target Version: v0.5.0 | Mode: Major (audit cleanup + code quality)

## Must have

- [ ] **NEXT** M1: Remove template scaffold from [`src/root.zig`](src/root.zig) — Delete `printAnotherMessage()`, `add()`, and the `basic add functionality` test; replace with meaningful VCS re-exports or empty module if unused
  - **Why critical (P0 #1)**: Auto-generated Zig template code misleads readers into thinking this is demo/placeholder code; undermines project credibility

- [ ] M2: Fix repository reference in [`install.sh`](install.sh) — Change `HOZ_REPO="edwardxie/hoz"` to `HOZ_REPO="EdwardJoke/hoz-vcs"`
  - **Why critical (P0 #2)**: Installation is completely broken; users cannot install hoz via this script with wrong repo URL

- [ ] M3: Address [`network/libservice.a`](src/network/libservice.a) — Either remove the opaque binary OR provide source code + build instructions in README
  - **Why critical (P0 #3)**: 2,264-byte precompiled binary with no provenance violates open-source transparency; could be security risk

## Should have

- [ ] S1: Replace 7 placeholder `expect(true)` tests with real unit tests:
  - [`src/cli/cli.zig`](src/cli/cli.zig): Test CLI argument parsing
  - [`src/network/network.zig`](src/network/network.zig): Test protocol negotiation
  - [`src/stash/stash.zig`](src/stash/stash.zig): Test stash save/restore logic
  - [`src/perf/perf.zig`](src/perf/perf.zig): Test bitmap/bloom filter operations
  - [`src/ci/ci.zig`](src/ci/ci.zig): Test CI config generation
  - [`src/history/history.zig`](src/history/history.zig): Test log formatting
  - [`src/reset/reset.zig`](src/reset/reset.zig): Test reset mode selection
  - **Why important (P1 #4)**: False test coverage gives illusion of quality; real tests catch regressions

- [ ] S2: Clean up [`build.zig.zon`](build.zig.zon) commented dependencies — Remove or restore the `.dependencies` block
  - **Why important (P1 #5)**: Comment residue confuses developers about actual dependency needs

- [ ] S3: Synchronize [`.wasup/wasup.toml`](.wasup/wasup.toml) version numbers — Update `current_version` to `v0.4.2`, set `next_version` to `v0.5.0`
  - **Why important (P1 #6)**: Stale version breaks release workflow automation

## Could have

- [ ] C1: Refactor `findCommand()` in [`src/main.zig`](src/main.zig) — Replace 40+ if-else chain with loop over `ALL_COMMANDS` array + `@field()` for enum construction
  - **Why nice to have (P2 #8)**: Eliminates command definition triplication (enum, findCommand, dispatch); reduces maintenance burden
  - **Note**: Requires verifying `ALL_COMMANDS` structure supports this pattern

- [ ] C2: Refactor `dispatch()` in [`src/cli/dispatcher.zig`](src/cli/dispatcher.zig) — Same refactoring as M1 for consistency
  - **Why nice to have**: Paired with C1; ensures both command resolution paths use same data-driven approach

## Won't have (this version)

- [ ] W1: Fix known test crashes from v0.4.1 todo (6 memory safety issues) — Deferred to v0.5.1 patch release; focus on audit findings first
- [ ] W2: Add new Git commands or features — Audit cleanup only; feature work comes after clean codebase
- [ ] W3: Performance optimization — Not in scope for this version; current focus is correctness and credibility
