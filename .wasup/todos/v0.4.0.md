# Todo
> Target Version: v0.4.0 | Mode: Feature (minor bump)

## Must have

- [ ] **NEXT** M1: Fix Io system API changes (21 errors) — `Io.Threaded.new()` removed in 0.16, `Io.init()` removed, `Io.Writer.interface` field gone. Affected files:
  - `src/object/io.zig` (3 errors): replace `Io.Threaded.new(...)` with 0.16 constructor pattern
  - `src/cli/branch.zig` (3 errors): same `Io.Threaded.new()` + `u2` range overflow
  - `src/cli/commit.zig` (1 error): `Io.Threaded.new()`
  - `src/cli/init.zig` (1 error): `Io.Threaded.new()`
  - `src/cli/output.zig` (8 errors): `Io.Writer.interface` field removed — rewrite writer dispatch
  - `src/branch/delete.zig` (2 errors): `Io.init()` removed
  - `src/branch/list.zig` (2 errors): `Io.init()` removed
  - `src/branch/upstream.zig` (2 errors): `Io.init()` removed + optional type mismatch
  - **Action**: Audit current Io struct definition in root.zig, map all 6 Io API surfaces to their 0.16 equivalents, fix all 21 call sites

- [ ] M2: Fix std.fs namespace reorganization (12 errors) — `struct 'fs' has no member named 'File'`. In 0.16, `std.fs.File` access pattern changed. Affected files:
  - `src/index/index.zig` (8 errors): lines 805, 834, 873, 908, 946, 994, 1032, 1064, 1102, 1172 — all `fs.File.open*` / `fs.File.*` calls
  - `src/index/index_entry.zig` (5 errors): lines 144, 178, 215, 252, 282 — same pattern
  - **Action**: Replace `fs.File` with correct 0.16 path (`std.fs.File` or direct file API usage)

- [ ] M3: Fix const-correctness issues (14 errors) — Zig 0.16 tightened const qualifiers on allocator returns and struct pointers. Affected files:
  - `src/tree/builder.zig` (7 errors): `gpa.allocator()` now returns `*const T`, need `&gpa` or mutable reference; plus `[20]u8` vs `OID` type mismatch at line 245
  - `src/checkout/switch.zig` (4 errors): `*const T` on allocator + `Allocator ==` comparison not allowed + `*anyopaque` vs null comparison at lines 160, 187, 197, 218
  - `src/branch/create.zig` (2 errors): `*const RefStore` where `*RefStore` expected at lines 87, 99
  - `src/branch/rename.zig` (2 errors): same `*const RefStore` issue at lines 109, 123
  - **Action**: Add `var` where needed, use `&gpa` instead of `gpa.allocator()`, fix pointer constness

- [ ] M4: Fix crypto module path change (1 error) — `crypto.hash.sha1` renamed/moved in 0.16:
  - `src/index/index.zig:805`: `struct 'crypto.hash' has no member named 'sha1'`
  - **Action**: Update import path from `crypto.hash.sha1` to `crypto.hash.Sha1` (or new 0.16 location)

- [ ] M5: Fix OID type and struct issues (6 errors) — OID struct definition drift and method removal:
  - `src/ref/ref.zig:100`: `OID.hex()` method missing — method was removed or renamed in 0.16
  - `src/ref/store.zig` (4 errors): lines 350, 360, 374, 388 — `missing struct field: io` — RefStore init literal missing field that 0.16 now requires
  - `src/tree/builder.zig:245`: `[20]u8` cannot coerce to `OID` type
  - **Action**: Audit OID struct definition, add missing `io` field to RefStore, replace `.hex()` call with hex formatter, fix array-to-OID conversion

- [ ] M6: Fix remaining type mismatches (3+ errors):
  - `src/cli/branch.zig:430`: `type 'u2' cannot represent integer value '6'` — enum/flag field too small
  - `src/branch/upstream.zig:215`: `expected '[]const u8', found '?[]const u8'` — optional unwrap needed
  - Any remaining stragglers after M1–M5 are addressed here
  - **Action**: Expand u2 field to u3+, add `orelse ""` or `.?` for optional, final sweep

## Should have

- [ ] S1: Fix runtime test failures (11 failing tests) — After compilation errors are resolved, run full test suite and diagnose each of the 11 runtime failures. Likely causes: deprecated std lib behavior changes, assertion updates, logic affected by API signature changes during M1–M6.
  - **Action**: Run `zig build test`, capture each failure stack trace, fix one by one

- [ ] S2: Investigate dylib crash (libssh2) — integration_test crashes with `libssh2.1.dylib not found`. This is likely an environment/brew issue but should be documented or worked around so CI can pass.

## Could have

- [ ] C1: Consolidate `resolveRef*()` variants (8 copies) — identified in v0.3.5 changelog as v0.4.0 candidate across `log.zig`, `archive.zig`, `rev_list.zig`, `pull.zig`, `request_pull.zig`, `ls_tree.zig`, `merge.zig`, `stash/save.zig`. If time permits after all Must-haves complete.

- [ ] C2: Consolidate `updateHead()` variants (3 copies) — across `commit.zig`, `head.zig` (canonical), `checkout/switch.zig`. Same v0.3.5 candidate.

## Won't have (defer to v0.5.0)

- [ ] W1: New git subcommands or features — this release is purely an API migration
- [ ] W2: Performance profiling or optimization — focus only on correctness
- [ ] W3: Full CLI framework refactor with base Command struct — deferred as noted since v0.3.1
