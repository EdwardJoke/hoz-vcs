# Todo
> Target Version: v0.3.5 | Mode: Bug

## Must have

- [ ] **NEXT** M1: Consolidate `readObject()` into shared `object/io.zig` — identify all 25+ call sites across CLI commands (reset, stash, blame, describe, checkout, clean), extract common OID→path→read→decompress logic into single reusable function; update all callers to import from new location; ensure error handling consistency; verify zero test regressions
- [ ] M2: Consolidate `resolveHead()` via `commit/head.zig` reuse — audit 8+ inline copies of HEAD resolution logic; replace each with import from existing proper implementation in commit/head.zig; normalize error handling differences between copies; validate branch/commit resolution still works correctly
- [ ] M3: Unify `writeLooseObject()` implementation — compare duplicated versions in commit.zig and filter_repo.zig; extract canonical version into shared location (e.g., object/writer.zig or object/io.zig); update both modules to delegate to unified function; ensure hash computation and file permission handling identical

## Should have

- [ ] S1: Extract `makeMockCommit()` to shared test helper — create src/testing/mock.zig with reusable mock commit builder; move identical ~15-line function body from fast_forward.zig and analyze.zig (merge tests) into shared module; update both test files to import from testing/mock.zig; verify merge test suite still passes
- [ ] S2: Extract empty tree creation helper in commit.zig — identify 4 duplicate blocks of empty tree pattern inside writeTree() function; create private helper method (e.g., writeEmptyTree()); replace all 4 occurrences with single call; reduce redundancy while keeping writeTree() readable

## Could have

- [ ] C1: Refactor GitCompatTester's 16 `run*()` methods — design generic runPairTest(git_cmd, hoz_cmd) helper that encapsulates setup/execute/compare/cleanup pattern; refactor all 16 methods (runCheckout, runSwitch, runMerge, etc.) to use generic helper; target ~300+ lines of boilerplate reduction in final/compat.zig; maintain backward compatibility with existing test assertions
- [ ] C2: Audit other command pairs for future merging — document which command groups could be consolidated (stash save/list/drop/apply → stash subcommands; tag create/delete/list → tag subcommands; remote add/remove/rename → remote subcommands); create structured wishlist in comments or separate planning doc for v0.3.6+ consideration; estimate duplication reduction potential for each group

## Won't have (this version / defer to v0.3.6)

- [ ] W1: Full CLI framework refactor with base Command struct/mixin — requires broader API design discussion; current command count (60+) makes this high-risk without thorough interface analysis; defer until after duplicate code cleanup reduces maintenance burden
