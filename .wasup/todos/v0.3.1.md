# Todo
> Target Version: v0.3.1 | Mode: Patch

## Must have
- [ ] **NEXT** M1: Enhance core Output module (`src/cli/output.zig`) with tree-nesting symbols (├── └── │), section dividers, and icon set for AI-parseable hierarchy — foundation all other modules depend on
- [ ] M2: Standardize Diff output (`src/diff/unified.zig`, `src/diff/result.zig`) — hunk headers with `@@` symbols, file change indicators (➕/➖/✏️), binary/submodule markers
- [ ] M3: Standardize Status output (`src/workdir/status.zig`, CLI status display) — file status icons (M/A/D/R/?/U) with indented nesting for staged/unstaged sections

## Should have
- [ ] S1: Standardize Log/Graph output (`src/history/graph.zig`, `src/history/pretty.zig`) — Unicode box-drawing graph lines (│ ├─ ╰─ ◉ ●) replacing plain ASCII, commit node symbols
- [ ] S2: Standardize Tree/LS output (`src/tree/ls_tree.zig`) — proper tree-nesting characters (├── └──) for recursive directory display
- [ ] S3: Standardize Branch/List output (`src/branch/list.zig`) — current branch marker (→ *), upstream ahead/behind indicators (↑↓)

## Could have
- [ ] C1: Standardize Stash/Tag/Remote list output (`src/stash/list.zig`, `src/tag/list.zig`, `src/remote/list.zig`) — consistent list item formatting with index badges
- [ ] C2: Standardize Submodule diff output (`src/diff/submodule.zig`) — submodule change symbols (⊞ Δ) with nested commit log preview

## Won't have (this version / defer to v0.4.0)
- [ ] W1: JSON output schema redesign with typed fields — needs broader API design discussion
- [ ] W2: Porcelain output mode expansion — low priority for current user base
