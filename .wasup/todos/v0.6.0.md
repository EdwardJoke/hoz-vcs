# Todo
> Target Version: v0.6.0 | Mode: Function

## Must have
- [ ] **NEXT** M1: Add JSON output mode to `Output` struct — all commands serialize structured data to JSON internally
- [ ] M2: Add toon display formatter — pipe JSON through `toon` CLI for default human-readable output
- [ ] M3: Keyword highlighting — commit hashes, branch names, file status codes, refs rendered with terminal color
- [ ] M4: Clean usage descriptions — rewrite help text for all commands to be short, scannable, with examples
- [ ] M5: Remove dead `util/format.zig` — already unused, delete file and its tests

## Should have
- [ ] S1: Native toon formatter in Zig — eliminate external `toon` CLI dependency for standalone use
- [ ] S2: toon output for `hoz log` — compact commit list with highlighted hashes, refs, and dates
- [ ] S3: toon output for `hoz status` — staged/unstaged/untracked sections with status code colors
- [ ] S4: toon output for `hoz diff` — file headers and hunk stats in toon format
- [ ] S5: toon output for `hoz branch` — branch list with current marker and upstream info

## Could have
- [ ] C1: `--json` flag to output raw JSON (skip toon formatting)
- [ ] C2: `--porcelain` flag for git-compatible machine output
- [ ] C3: Progress indicators using toon format for clone/fetch/push
- [ ] C4: Terminal width detection for responsive output

## Won't have (this version)
- [ ] W1: Interactive TUI mode
- [ ] W2: Graph visualization for log
- [ ] W3: Custom toon themes/color schemes
