# Todo
> Target Version: v0.3.2 | Mode: Patch

## Must have

- [ ] **NEXT** M1: Fix `Identity.parse()` whitespace handling bug in [commit.zig](src/object/commit.zig) — trim trailing space from name before `<`, skip empty tokens when splitting timestamp/tz; fixes 7 commit parse + 8 tag parse + 3 identity unit test = **18 failures** (the single largest root cause)
- [ ] M2: Fix OID test data and `fromHex()` edge cases in [oid.zig](src/object/oid.zig) — correct odd-length hex strings to even length (39→40 chars), fix `short()` output expectations, fix `toHexLen()` return type; fixes **7 failures + 2 ABRT crashes** in blob OID tests
- [ ] M3: Fix `Object.parse()` error type mismatches in [object.zig](src/object/object.zig) — update test expectations to match actual Zig `parseInt` behavior (`Overflow` for negative size, `InvalidCharacter` for float/hex sizes, `InvalidObjectFormat` for tab delimiter), or add error mapping layer; fixes **9 object_fuzz_test failures** + 1 computeHeaderSize failure
- [ ] M4: Fix `Tree.parse()` hardcoded allocator and memory leaks in [tree.zig](src/object/tree.zig) — accept `allocator` parameter instead of hardcoding `std.testing.allocator`, add proper `defer` cleanup in all tree parse tests; fixes **11 leaks + 2 failures** (bad mode error type, truncated entry)

## Should have

- [ ] S1: Replace `dummyGetCommit` null-stub with real mock commit objects in [fast_forward.zig](src/merge/fast_forward.zig) — create `MockCommit` struct with realistic fields (tree OID, parents, message), use it in all 5 FastForwardChecker tests to validate actual fast-forward detection logic
- [ ] S2: Replace `dummyGetCommit` null-stub with real mock commit objects in [analyze.zig](src/merge/analyze.zig) — same MockCommit pattern for all 3 MergeAnalyzer tests, validate merge analysis logic (up_to_date, is_normal, can_fast_forward, etc.)
- [ ] S3: Replace placeholder `expect(true)` test in [reflog.zig](src/ref/reflog.zig) line 283 — implement real ReflogEntry format validation (parse a formatted entry string, verify all fields roundtrip correctly)

## Could have

- [ ] C1: Add integration-style edge-case tests for `Identity.parse()` — empty name, unicode characters in name/email, extra spaces between tokens, missing timezone
- [ ] C2: Add roundtrip test verifying v0.3.1 output formatting doesn't regress — capture Output module symbol rendering, verify tree/node/status icon consistency across calls

## Won't have (this version / defer to v0.4.0)

- [ ] W1: Comprehensive JSON output schema test suite — needs broader API design discussion first
- [ ] W2: Fuzz testing infrastructure with corpus management — requires build system changes
